# MagicSquare — D-LOC GREEN (Minimal) Report

**프로젝트:** MagicSquare_1004  
**문서:** 05. D-LOC-01 GREEN · `find_blank_coords` 최소 구현  
**일자:** 2026-06-04  
**브랜치:** `green`  
**근거:** `/green-minimal` · `Report/04.MagicSquare_D_LOC_RED_Skeleton_Report.md` · `magic-square-tdd` Skill  
**연관:** `src/entity/blank_loc.py` · `tests/entity/test_d_loc_01.py`

---

## 1. 요약

**D-LOC-01** RED 스켈레톤을 **GREEN**으로 전환했다. `src/entity/blank_loc.py`에 `find_blank_coords()` 최소 구현을 추가하고, 테스트의 `pytest.fail`을 **assert**로 교체해 `pytest` **PASS**를 확인했다.

| 산출 | 상태 |
|---|---|
| `find_blank_coords` | ✅ GREEN (entity) |
| `test_d_loc_01_blank_coords_row_major` | ✅ PASS |
| D-LOC-02 · D-LOC-03 | ⬜ 별도 사이클 |
| git commit | ⬜ 사용자 요청 시 |

---

## 2. TDD 선언

```
Phase: green | Layer: entity | Track: Logic | Test: D-LOC-01
```

| 단계 | 결과 |
|---|---|
| RED 재확인 | `Failed: RED: D-LOC-01 — 구현 없음, 의도적 실패` |
| GREEN 구현 | `blank_loc.find_blank_coords` |
| PASS | `1 passed` (단일 테스트 · 파일 전체) |

---

## 3. 구현 요약

### 3.1 `find_blank_coords(grid)`

| 항목 | 내용 |
|---|---|
| **스캔** | `row` 0..`GRID_SIZE-1`, `col` 0..`GRID_SIZE-1` (row-major) |
| **조건** | `grid[row][col] == BLANK_CELL` |
| **반환** | `list[tuple[int,int]]` — **1-index** `(row + INDEX_BASE, col + INDEX_BASE)` |

### 3.2 SSOT (`src/entity/constants.py`)

| 상수 | 값 |
|---|---|
| `GRID_SIZE` | 4 |
| `MAGIC_CONSTANT` | 34 |
| `MAX_CELL_VALUE` | 16 |
| `BLANK_CELL` | 0 |
| `BLANK_COUNT` | 2 |
| `INDEX_BASE` | 1 |

매직넘버 리터럴 없음. `34`/`16`/`4`/`0`은 constants만 사용.

### 3.3 ECB · 오류

| 체크 | 결과 |
|---|---|
| entity → boundary/control import | 없음 |
| E001~E005 raise/return | 없음 |
| Domain Mock | 없음 (실제 `grid_g1`) |

---

## 4. 테스트

### 4.1 G1 · Then

```text
[[16, 3,  2, 13],
 [ 5,  0, 11,  8],
 [ 9,  6,  0, 12],
 [ 4, 15, 14,  1]]
```

**기대:** `[(2, 2), (3, 3)]` (1-index, row-major)

### 4.2 pytest

```bash
python -m pytest tests/entity/test_d_loc_01.py::test_d_loc_01_blank_coords_row_major -v
python -m pytest tests/entity/test_d_loc_01.py -v
```

→ **1 passed** (각각)

### 4.3 REPL 스모크

```python
find_blank_coords(G1)  # → [(2, 2), (3, 3)]
```

---

## 5. 변경 파일

| 파일 | 변경 |
|---|---|
| `src/entity/constants.py` | `BLANK_CELL`, `BLANK_COUNT`, `INDEX_BASE` 추가 |
| `src/entity/blank_loc.py` | **신규** — `find_blank_coords` |
| `tests/entity/test_d_loc_01.py` | `pytest.fail` → `assert result == [(2, 2), (3, 3)]` |

**범위 외 (미변경):** D-LOC-02/03 · control/boundary · REFACTOR

---

## 6. 다음 단계

| 순서 | 작업 |
|---|---|
| 1 | PRD §6 **FR-LOC-01** 문구 반영 |
| 2 | RED → GREEN — **D-LOC-02** (invariant), **D-LOC-03** (동일 행 좌→우) |
| 3 | `docs/TDD_RED_TODO.md` · README D-LOC `[x]` 갱신 |
| 4 | (선택) `green` 브랜치 GitHub 푸시 · PR |

---

## 7. 참고

- `Report/04.MagicSquare_D_LOC_RED_Skeleton_Report.md`
- `Prompting/06. MagicSquare_1004_Export_Transcript.md`
- `.cursor/skills/magic-square-tdd/SKILL.md`
