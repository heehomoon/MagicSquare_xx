# MagicSquare — D-LOC RED Skeleton Report

**프로젝트:** MagicSquare_1004  
**문서:** 04. D-LOC-01 RED 스켈레톤 · pytest · Harness 이슈  
**일자:** 2026-06-04  
**근거:** `/red-skeleton` · `Report/03.MagicSquare_TDD_RED_Planning_Report.md` · `magic-square-tdd` Skill  
**연관:** `tests/entity/test_d_loc_01.py` · `tests/conftest.py` · `src/entity/constants.py`

---

## 1. 요약

**D-LOC-01** (entity · Logic Track) RED 스켈레톤을 추가하고 `pytest` **의도적 FAIL**을 확인했다.  
`find_blank_coords` 도메인 로직은 미구현이며, Then은 `pytest.fail("RED: D-LOC-01 — …")` 한 줄만 사용한다.

| 산출 | 상태 |
|---|---|
| `tests/entity/test_d_loc_01.py` | ✅ RED 스켈레톤 |
| `tests/conftest.py` · `grid_g1` (G1) | ✅ |
| `src/entity/constants.py` | ✅ SSOT 상수만 |
| `find_blank_coords` | ⬜ GREEN 대기 |
| D-LOC-02 · D-LOC-03 | ⬜ 미착수 |

---

## 2. TDD 선언

```
Phase: red | Layer: entity | Track: Logic | Test: D-LOC-01
```

| 항목 | 값 |
|---|---|
| **Test ID** | D-LOC-01 |
| **FR** | FR-LOC-01 (PRD 미기재 — Report 03 합성 초안) |
| **대상 함수 (GREEN)** | `find_blank_coords(grid)` (이름 확정) |
| **pytest** | `python -m pytest tests/entity/test_d_loc_01.py::test_d_loc_01_blank_coords_row_major -v` |
| **결과** | `Failed: RED: D-LOC-01 — 구현 없음, 의도적 실패` (exit 1) |

---

## 3. Fixture G1

4×4, 빈칸 `0` **2개**, row-major 1-index **(2,2)** → **(3,3)**.

```text
[[16, 3,  2, 13],
 [ 5,  0, 11,  8],
 [ 9,  6,  0, 12],
 [ 4, 15, 14,  1]]
```

| 항목 | 값 |
|---|---|
| **Then (GREEN 목표)** | `[(2, 2), (3, 3)]` (1-index, row-major) |
| **SSOT import** | `entity.constants` — `GRID_SIZE`, `MAGIC_CONSTANT`, `MAX_CELL_VALUE` |

---

## 4. 생성·변경 파일

| 파일 | 역할 |
|---|---|
| `tests/entity/test_d_loc_01.py` | AAA 주석 · `pytest.fail` RED |
| `tests/conftest.py` | `grid_g1` · `src` path 우선 · `entity` 모듈 캐시 정리 |
| `src/entity/constants.py` | `4` / `34` / `16` SSOT (로직 없음) |
| `tests/entity/__init__.py` | **삭제** — pytest가 `tests/entity`를 `entity` 패키지로 오인 |

### 4.1 RED 규칙 준수

- assert 본문 · skip · xfail · 통과 더미 **없음**
- Domain Mock **없음**
- entity E001~E005 **없음**

---

## 5. Harness 이슈 — `tests/entity` vs `src/entity`

| 증상 | 원인 | 조치 |
|---|---|---|
| `No module named 'entity.constants'` | `tests/entity/__init__.py` 존재 시 `entity`가 테스트 패키지로 shadow | `tests/entity/__init__.py` **삭제** |
| `No module named 'entity.test_d_loc_01'` | 동일 — 테스트 모듈이 `entity` 하위로 수집 | 위와 동일 |
| conftest import 실패 (잔여) | `sys.modules`에 잘못된 `entity` 캐시 | `tests/conftest.py`에서 `src` 선삽입 + `entity*` 모듈 제거 후 import |

**권고:** 장기적으로 `tests/entity/` 디렉터리명을 `tests/logic_entity/` 등으로 바꾸거나, pytest `pythonpath`·패키지 layout 문서화.

---

## 6. `src/entity/constants.py` 범위

`/red-skeleton` 지시는 `src/` 로직 금지였으나, 픽스처 규칙 **`entity/constants.py` import** 를 만족하기 위해 **상수 파일만** 추가했다.  
`find_blank_coords` · `blank_loc` 모듈은 **미추가**.

---

## 7. 다음 단계

| 순서 | 작업 |
|---|---|
| 1 | PRD §6에 **FR-LOC-01** 반영 |
| 2 | GREEN — `find_blank_coords` 구현 · `pytest.fail` → assert `[(2,2),(3,3)]` |
| 3 | RED — D-LOC-02 (invariant) · D-LOC-03 (동일 행 좌→우) |
| 4 | `docs/TDD_RED_TODO.md` · README 체크리스트 D-LOC 항목 `[x]` 갱신 |

---

## 8. 참고

- `Report/03.MagicSquare_TDD_RED_Planning_Report.md`
- `Prompting/05. MagicSquare_1004_Export_Transcript.md`
- `docs/TDD_RED_TODO.md`
- `.cursor/skills/magic-square-tdd/SKILL.md`
